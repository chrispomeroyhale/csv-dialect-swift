public typealias Field = String
public typealias Row = [Field]
/**
    A header is a non-record row.
*/
public typealias Header = Row
/**
    A record is a non-header row.
*/
public typealias Record = Row
